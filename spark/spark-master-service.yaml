apiVersion: v1
kind: Service

metadata:
  name: spark-master
  namespace: video-pipeline

spec:
  selector:
    app: spark-master
  
  ports:
  - name: spark
    port: 7077
    targetPort: spark

  - name: web-ui
    port: 8080
    targetPort: web-ui

  type: ClusterIP