apiVersion: v1
kind: Secret

# Lab credentials. Change all four values before this runs anywhere that is
# not a local cluster. A new secret key can be generated with:
#   openssl rand -base64 42

metadata:
  name: superset-secrets
  namespace: video-pipeline

type: Opaque

stringData:
  SUPERSET_SECRET_KEY: PLEASE_CHANGE_ME_uJ8kQ2mZ1xVbN5rT9wS3
  ADMIN_PASSWORD: admin
  METADB_USER: superset
  METADB_PASSWORD: superset
