apiVersion: v1
kind: Service

metadata:
  name: kafka-bootstrap
  namespace: video-pipeline

spec:

  selector:
    app: kafka
  
  ports:

  - name: broker
    port: 9092
    targetPort: broker
  
  type: ClusterIP