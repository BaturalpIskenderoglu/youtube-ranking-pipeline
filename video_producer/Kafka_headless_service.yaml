apiVersion: v1
kind: Service

metadata:
  name: kafka
  namespace: video-pipeline

spec:
  clusterIP: None

  selector:
    app: kafka
  
  ports:
  
  - name: broker
    port: 9092
    targetPort: broker

  - name: controller
    port: 29093
    targetPort: controller