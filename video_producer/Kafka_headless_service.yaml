apiVersion: v1
kind: Service

metadata:
  name: kafka_headless
  namespace: video_pipeline

spec:
  clusterIP: None

  selector:
    app: kafka
  
  ports:
  
  - name: broker
    port: 9092
    targetPort: broker

  - name: controller
    port: 29093
    targetPort: controller